# ==============================================
# CACTUS ANALYTICS SERVICE - Environment Variables
# ==============================================

# Note: This service currently does not use environment variables.
# Port configuration is done via command line argument:
# uvicorn main:app --host 0.0.0.0 --port 3002

# If you need to configure CORS origins in production,
# modify the CORSMiddleware in main.py directly.

