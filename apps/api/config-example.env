# ========================================
# CONFIGURACIÓN DEL SISTEMA DE COMPARACIÓN MENSUAL
# ========================================

# Base de datos PostgreSQL
# Note: Docker Compose uses port 5433 to avoid conflict with local PostgreSQL (5432)
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/CRM

# ========================================
# CONFIGURACIÓN DE ARCHIVOS
# ========================================

# Tamaño máximo de archivos (en bytes)
MAX_FILE_SIZE=104857600  # 100MB

# Extensiones permitidas (separadas por comas)
ALLOWED_EXTENSIONS=.xlsx,.xls

# ========================================
# CONFIGURACIÓN DE COMPARACIÓN MENSUAL
# ========================================

# Tamaño máximo específico para comparación mensual
COMPARACION_MENSUAL_MAX_FILE_SIZE=104857600  # 100MB

# Porcentaje para warning de registros sin asesor
COMPARACION_MENSUAL_WARNING_SIN_ASESOR_PERCENT=50

# Tamaño de lote para procesamiento
COMPARACION_MENSUAL_BATCH_SIZE=1000

# Timeout para procesamiento (en ms)
COMPARACION_MENSUAL_TIMEOUT=300000  # 5 minutos

# ========================================
# CONFIGURACIÓN DE SNAPSHOTS
# ========================================

# Días de retención de snapshots
SNAPSHOT_RETENTION_DAYS=365

# Habilitar verificación de integridad
ENABLE_SNAPSHOT_INTEGRITY_CHECK=true

# ========================================
# CONFIGURACIÓN DE AUDITORÍA
# ========================================

# Habilitar logging de auditoría
ENABLE_AUDIT_LOGGING=true

# Nivel de log para auditoría
AUDIT_LOG_LEVEL=info

# ========================================
# CONFIGURACIÓN DE EXPORTACIÓN
# ========================================

# Máximo de registros para exportación
EXPORT_MAX_RECORDS=100000

# Incluir metadatos en exportación
EXPORT_INCLUDE_METADATA=true

# ========================================
# CONFIGURACIÓN DE UI
# ========================================

# Paginación por defecto
UI_PAGINATION_DEFAULT=100

# Máximo de paginación
UI_PAGINATION_MAX=1000

# Mostrar warnings en UI
UI_SHOW_WARNINGS=true

# Requerir confirmación para cambios críticos
UI_CONFIRMATION_REQUIRED=true

# ========================================
# CONFIGURACIÓN DE PERFORMANCE
# ========================================

# Habilitar índices optimizados
ENABLE_OPTIMIZED_INDEXES=true

# Cache de consultas frecuentes
ENABLE_QUERY_CACHE=true

# ========================================
# CONFIGURACIÓN DE SEGURIDAD
# ========================================

# Secret para firmar tokens JWT (debe ser único y seguro en producción)
JWT_SECRET=dev-insecure-secret-change-me

# Habilitar validación estricta
ENABLE_STRICT_VALIDATION=true

# ========================================
# GOOGLE OAUTH & CALENDAR INTEGRATION
# ========================================

# Google OAuth Client ID (from Google Cloud Console)
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Google OAuth Client Secret (from Google Cloud Console)
GOOGLE_CLIENT_SECRET=your-client-secret

# Google OAuth Redirect URI (CRÍTICO: must match Google Cloud Console EXACTLY)
# This URI must match EXACTLY (case-sensitive, no trailing slash) with one of the
# "Authorized redirect URIs" configured in Google Cloud Console.
#
# For development:
#   - Google Cloud Console: http://localhost:3001/v1/auth/google/callback
#   - This variable: http://localhost:3001/v1/auth/google/callback
#
# For production:
#   - Google Cloud Console: https://your-domain.com/v1/auth/google/callback
#   - This variable: https://your-domain.com/v1/auth/google/callback
#
# Common mistakes to avoid:
#   - ❌ Trailing slash: http://localhost:3001/v1/auth/google/callback/
#   - ❌ Wrong protocol: https://localhost:3001/v1/auth/google/callback (dev)
#   - ❌ Wrong path: http://localhost:3001/v1/auth/google/callbacks
#   - ✅ Correct: http://localhost:3001/v1/auth/google/callback
#
# If you get "Error 400: redirect_uri_mismatch", verify:
#   1. This value matches EXACTLY with Google Cloud Console
#   2. No trailing slash
#   3. Correct protocol (http for dev, https for prod)
#   4. Path is exactly /v1/auth/google/callback
GOOGLE_REDIRECT_URI=http://localhost:3001/v1/auth/google/callback

# Encryption key for storing Google OAuth tokens (MUST be at least 32 characters)
# Generate with: openssl rand -base64 32
GOOGLE_ENCRYPTION_KEY=change-me-to-a-random-32-char-string-or-longer

# Frontend URL for OAuth redirects
# For development: http://localhost:3000
# For production: https://your-domain.com
FRONTEND_URL=http://localhost:3000

# Requerir autenticación para operaciones críticas
REQUIRE_AUTH_FOR_CRITICAL_OPERATIONS=true

# ========================================
# CONFIGURACIÓN DE CORS
# ========================================

# Orígenes permitidos para CORS (separados por comas)
# En desarrollo: http://localhost:3000
# En producción: https://tu-dominio.com,https://www.tu-dominio.com
CORS_ORIGINS=http://localhost:3000

# Puerto del servidor API
PORT=3001

# ========================================
# INSTRUCCIONES DE USO
# ========================================

# 1. Copia este archivo como .env en apps/api/
# 2. Ajusta las variables según tu entorno
# 3. Especialmente importante: DATABASE_URL
# 4. Reinicia la aplicación después de cambios

# ========================================
# EJEMPLOS DE DATABASE_URL
# ========================================

# PostgreSQL local (Docker Compose - default):
# DATABASE_URL=postgresql://postgres:postgres@localhost:5433/CRM

# PostgreSQL local (si usas instalación local en puerto 5432):
# DATABASE_URL=postgresql://usuario:password@localhost:5432/nombre_db

# PostgreSQL con usuario específico:
# DATABASE_URL=postgresql://maatwork_user:maatwork_pass@localhost:5433/CRM

# PostgreSQL remoto:
# DATABASE_URL=postgresql://usuario:password@servidor.com:5432/maatwork_db



