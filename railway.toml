# Root Railway configuration for monorepo
# Railway will detect services from apps/*/railway.toml files

[railway]
# This file acts as the root configuration
# Individual services are configured in their respective app directories
